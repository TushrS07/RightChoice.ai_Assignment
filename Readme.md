# Fetch Users from Public API (Beginner Version)
This is a simple Python script that fetches user data from a public API and prints details like **name**, **username**, **email**, and **city**.

## Repository Contents
- **Assignment.py** 
    - Fetches data from the API and prints it in the required format.  
- **Assignment_Bonus.py** — Handles the bonus task:
    - Prints only users whose city name starts with **'S'**
    - Handles API errors (like failed responses or empty data)


## Requirements
- Python 3  
- `requests` library  

---

### Install the `requests` library:
```bash
pip install requests
```

---

## How to Run:

- Assignment.py -
```bash
python Assignment.py
```
- Assignment_Bonus.py - 
```bash
python Assignment_Bonus.py
```